# =============================================================================
# AZURE SQL DATABASE CONFIGURATION (Required for backend)
# =============================================================================
# These values come from the Bicep deployment output
# Authentication is handled via Azure AD (az login)

# The SQL Server hostname (e.g., timesheet-sql-xyz123.database.windows.net)
DB_SERVER=your-server.database.windows.net

# The database name (default: timesheetdb)
DB_NAME=timesheetdb

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# API server port (default: 3001)
# PORT=3001

# Log level: debug, info, warn, error (default: info)
# LOG_LEVEL=debug

# Frontend API URL (default: http://localhost:3001/api)
# VITE_API_URL=http://localhost:3001/api
