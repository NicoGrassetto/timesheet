# Azure Cosmos DB Configuration
# Create an Azure Cosmos DB account and get these values from Azure Portal

# Cosmos DB endpoint URL (from Azure Portal > Cosmos DB > Keys)
COSMOS_DB_ENDPOINT=https://your-account.documents.azure.com:443/

# Database name (will be created if it doesn't exist)
COSMOS_DB_DATABASE_NAME=timesheetdb

# Container names (will be created if they don't exist)
COSMOS_DB_PROJECTS_CONTAINER=projects
COSMOS_DB_ENTRIES_CONTAINER=entries

# Azure AD Service Principal Configuration
# Create a service principal and assign it to your Cosmos DB account
# https://docs.microsoft.com/en-us/azure/cosmos-db/how-to-setup-rbac

# Application (client) ID
AZURE_CLIENT_ID=your-client-id

# Client secret value
AZURE_CLIENT_SECRET=your-client-secret

# Directory (tenant) ID
AZURE_TENANT_ID=your-tenant-id

# Server Configuration
# Port for the API server
PORT=3001
